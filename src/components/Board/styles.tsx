import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  boardBox: {
    gap: 8,
  },
  infoTxt: {
    fontSize: 18,
    fontWeight: '300',
  },
});

export default styles;
